DROP TABLE IF EXISTS USERS;
CREATE SEQUENCE IF NOT EXISTS SEQ_USER START WITH 1;
CREATE TABLE users (ID INT PRIMARY KEY AUTO_INCREMENT, EMAIL VARCHAR(50), PASSWORD VARCHAR(30), TYPE VARCHAR(50));
INSERT INTO users (EMAIL, PASSWORD, TYPE) VALUES ('per@yahoo.no', 'password', 'STUDENT' );
COMMIT;